#! /bin/sh
version=001

echo "installer version" $version

install_archive=$1

#unpack here
echo "install archive :" $install_archive
tar -jxf $install_archive

[ ! -e $HOME/.m-1 ] && cp -R m-1/conf/.m-1 $HOME
exit
echo done
exit
